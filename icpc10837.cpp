#include <iostream>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int k, c;
    cin >> k >> c;
    for(int i = 0; i < c; i++){
        int n, m;
        cin >> n >> m;
    }
}